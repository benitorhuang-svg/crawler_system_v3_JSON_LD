# 測試與驗證指南 (Testing Guide)

本文件說明如何使用 `test/` 目錄下的工具來驗證爬蟲數據品質、系統流轉以及進行日常維運。

---

## 🚀 核心測試與維運工具

### 1. 統一回歸測試 (`test/scripts/regression_all_platforms.py`)
用於模擬真實採集流程並驗證各平台數據是否正確存入資料庫。

- **執行**: `uv run test/scripts/regression_all_platforms.py [all|104|1111|...]`

### 2. AI 基準測試 (`test/scripts/benchmark_ollama.py`)
量化 AI 辨識能力。讀取 `test/fixtures/data/` 中的 L1 數據作為真理來源。

- **執行**: `uv run test/scripts/benchmark_ollama.py`

### 3. 對抗性樣本生成 (`test/scripts/gen_adversarial_samples.py`)
利用 AI 生成損壞的數據樣本，主動測試系統的魯棒性。

### 4. 數據檢查工具 (`test/scripts/inspect_data.py`)
直接查看指定 URL 的解析結果、平台健康狀態或資料品質報告。

- **執行**: `uv run test/scripts/inspect_data.py`

---

## 📂 目錄結構說明 (SDD-Aligned)

| 資料夾 | 描述 | 使用場景 |
| :--- | :--- | :--- |
| **`test/fixtures/data/`** | **真理來源 (Ground Truth)** | 存放固化的 JSON/HTML 樣本。嚴禁隨意更改或刪除。 |
| **`test/scripts/`** | **維運工具** | 包含初始化、重置、同步、審核等所有指令腳本。 |
| **`test/fixtures/data/failed_samples/`** | **偵錯快照** | 臨時生成的 LOG、HTML 快照、AI 損壞樣本。 |
| **`test/system/`**| **整合測試** | 驗證完整流轉與資料庫互動。 |

---

## 🛠️ 常用維運指令

- **管理資料庫 (初始化/重置)**: `uv run test/scripts/manage_db.py`
- **查看數據品質報告**: `uv run test/scripts/inspect_data.py`
- **初始化標準目錄**: `uv run test/scripts/seed_std_categories.py`

---

## 🧪 開發者規範 (SDD Notes)

1. **樣本日導向**: 單元測試必須優先使用 `test/fixtures/data/` 中的靜態資料。
2. **防污染原則**: 測試 AI 功能時，務必確保數據不會寫入正式生產表的 L1 區域。
3. **隔離執行**: 基準測試建議在開發環境離線執行，避免影響生產抓取效能。
